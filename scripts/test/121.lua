


local function Return(windows,windowLength,maxinWindow)

    local BooleanTable = {}

    local Collection = {}

    for i,v in pairs(windows) do
        table.insert(BooleanTable,)
        if #Collection < mg then

        end
    end
end

-- example if checking value 6, checks the position and 3 posiitons behind it
print(Return({1,3,4,6},4,2))


-- true true false true